# Variant: Naive RAG (BM25 + direct LLM, no graph)
_base: "config/base.yaml"
variant_name: "baseline_naive_rag"
confidence:
  enabled: false
community:
  strategy: "leiden"
traversal:
  strategy: "fixed_hop"
  fixed_hop:
    k: 0   # no graph traversal
parallel:
  enabled: false
use_graph: false
